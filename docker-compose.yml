version: "3.9"

services:
  db:
    build: ./db/
    expose:
      - 5432
    restart: always
    environment:
      - POSTGRES_PASSWORD=Trustno1

  api:
    build: ./api/
    expose:
      - 3000
    ports:
      - 3000:3000
    environment:
      - ROCKET_ADDRESS=0.0.0.0
      - ROCKET_PORT=3000
    depends_on:
      - db

  front:
    build: ./front/
    ports:
      - 8080:80
    depends_on:
      - api
